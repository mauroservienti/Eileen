@model DeleteAuthorBookViewModel

@{
    ViewBag.Title = $"Cancella {Model.BookTitle} di {Model.AuthorName}";
}

<h2>Sei sicuro di voler cancellare il libro @Model.BookTitle di @Model.AuthorName?</h2>

@using(@Html.BeginForm(FormMethod.Post))
{
    <input type="submit" value="Cancella"/>
}